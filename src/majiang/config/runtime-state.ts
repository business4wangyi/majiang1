export let AUTO_PLAY_MODE = false;
export let DEBUG_MODE = false;

export function setAutoPlayMode(enabled: boolean): void {
  AUTO_PLAY_MODE = enabled;
}

export function setDebugMode(enabled: boolean): void {
  DEBUG_MODE = enabled;
}
