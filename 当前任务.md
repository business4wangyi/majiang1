# 当前任务

## 任务目标

基于 [`docs/majiang/UI_TECHNICAL_DESIGN.md`](/Users/felixfan/Desktop/AIUse/majiang1-ui/docs/majiang/UI_TECHNICAL_DESIGN.md) 持续推进麻将首版 UI 落地，并保持以下目标不变：

- 首版 UI 宿主为仓内 Web 页面
- MVP 范围只包含 `Lobby`、`Table`、`ResultModal`
- 跑通 1 人 + 3 AI 的单机麻将流程
- 复用现有 `src/majiang/core/` 与 `src/majiang/strategy/`
- 不影响现有 CLI 版本麻将功能

依据规范：

- [`AGENTS.md`](/Users/felixfan/Desktop/AIUse/majiang1-ui/AGENTS.md)
- [`docs/standards/rules/development.md`](/Users/felixfan/Desktop/AIUse/majiang1-ui/docs/standards/rules/development.md)
- [`docs/majiang/FOLDER_STRUCTURE.md`](/Users/felixfan/Desktop/AIUse/majiang1-ui/docs/majiang/FOLDER_STRUCTURE.md)

说明：

- `FOLDER_STRUCTURE.md` 仅作为目录规范目标，不视为当前代码已完全达到的事实

## 当前实现情况

### 已完成

- 已完成 Phase 0 / 1 / 2 的最小落地：
  - Web 会话路径已与 CLI 顶层输入依赖隔离
  - `game-session.ts` / `game-state-mapper.ts` 已落地
  - Web 宿主与静态页面已落地
  - MVP 页面已收敛为 `Lobby`、`Table`、`ResultModal`
- 已补充 CLI / Web 两条 smoke 验证链：
  - `npm run majiang:smoke`
  - `npm run majiang:web-smoke`
  - `npm run majiang:verify`
- 已补充页面交互收敛：
  - 状态横幅
  - 当前行动 / 庄家 / 圈数 / 摸牌次数展示
  - 请求进行中态
  - 底部操作提示文案
  - 结果弹层最小细节
- 已完成本轮真实页面主链路验收：
  - `Lobby` 可进入 `Table`
  - 已跑通至少一条“人类出牌 -> AI 自动推进 -> 再次回到人类回合”链路
  - 已在当前代码状态下复核 `ResultModal`、“再来一局”与控制台错误/警告为 0 的完整闭环

### 已完成的真实页面验收

已在可监听端口环境中完成并有快照支撑的真实浏览器验收：

- 打开 `http://127.0.0.1:4010/majiang-web`
- 确认 `Lobby` 初始展示正常
- 点击“开始新对局”，成功切换到 `Table`
- 确认头部状态信息展示正常
- 确认四家布局、中央牌桌、弃牌区展示正常
- 实际点击一张人类手牌，跑通：
  - 人类出牌
  - AI 自动推进
  - 回到人类回合
- 继续推进完整对局，成功展示 `ResultModal`
- 点击“再来一局”，成功关闭结果弹层并回到 `PLAYING`
- 补充 `favicon.ico` 后，本轮通过 Codex 内置浏览器确认麻将页面控制台错误/警告为 0

验收产物位于：

- [`/.playwright-cli/page-2026-04-25T09-34-06-999Z.png`](/Users/felixfan/Desktop/AIUse/majiang1-ui/.playwright-cli/page-2026-04-25T09-34-06-999Z.png)
- [`/.playwright-cli/page-2026-04-25T09-50-39-902Z.png`](/Users/felixfan/Desktop/AIUse/majiang1-ui/.playwright-cli/page-2026-04-25T09-50-39-902Z.png)
- [`/.playwright-cli/page-2026-04-25T09-46-16-761Z.yml`](/Users/felixfan/Desktop/AIUse/majiang1-ui/.playwright-cli/page-2026-04-25T09-46-16-761Z.yml)
- [`/.playwright-cli/page-2026-04-25T09-50-01-956Z.yml`](/Users/felixfan/Desktop/AIUse/majiang1-ui/.playwright-cli/page-2026-04-25T09-50-01-956Z.yml)
- [`/.playwright-cli/page-2026-04-25T12-25-51-851Z.yml`](/Users/felixfan/Desktop/AIUse/majiang1-ui/.playwright-cli/page-2026-04-25T12-25-51-851Z.yml)
- [`/.playwright-cli/page-2026-04-25T12-28-14-901Z.yml`](/Users/felixfan/Desktop/AIUse/majiang1-ui/.playwright-cli/page-2026-04-25T12-28-14-901Z.yml)
- [`/.playwright-cli/page-2026-04-25T12-28-40-458Z.png`](/Users/felixfan/Desktop/AIUse/majiang1-ui/.playwright-cli/page-2026-04-25T12-28-40-458Z.png)

### 当前剩余未完成项

- 视觉与交互仍偏 MVP，可继续做体验打磨
- 已新增 Web 运行时日志静默开关，`MajiangGameSession` 会关闭底层麻将 console/file 日志输出；`majiang:web-smoke` 与真实页面验收均已确认日志输出干净
- 若继续推进非 MVP 能力，需要先更新 `docs/majiang/UI_TECHNICAL_DESIGN.md` 的范围边界

## 本轮执行记录

### 已完成

- 新增 `当前任务.md`
- 同步任务目标、当前实现情况、真实页面验收进展、剩余问题
- 补充 `favicon.ico` 并确认控制台不再出现 404
- 修复响应动作时头部提示优先级
- 修复结果态状态横幅被普通出牌成功文案覆盖的问题
- 已重新复核 `ResultModal` 与“再来一局”真实浏览器闭环
- 已将当前真实页面验收结果回写 `docs/majiang/UI_TECHNICAL_DESIGN.md`

### 下一步

1. 按 MVP 范围继续打磨牌桌视觉密度与移动端细节
2. 如需新增非 MVP 能力，先更新设计文档边界再实现

## 新一轮执行记录

### 进行中

- 继续处理 MVP 内视觉密度与移动端细节
- 保持页面范围仍为 `Lobby`、`Table`、`ResultModal`
- 不新增 `RuleDrawer`、`DebugPanel`、时间线、动画等非 MVP 能力

### 新增改动

- `src/majiang/web/public/index.html`
  - `ResultModal` 补充 `role="dialog"`、`aria-modal` 与 `aria-labelledby`
- `src/majiang/web/public/styles.css`
  - 优化小屏布局密度、触控按钮宽度、座位圆角与牌面尺寸
  - 限制明牌/弃牌区域高度并允许内部滚动，避免长对局把牌桌撑得过高

### 验证结果

- `npm run majiang:web-smoke`
  - 通过
- `npm run majiang:verify`
  - 通过

### 本轮新增改动

- `src/majiang/runtime/runtime-context.ts`
  - 新增 `consoleLoggingEnabled`，默认 `true`，保证 CLI 仍保留原控制台日志行为
- `src/majiang/tools/logger.ts`
  - `console.log(...)` 受 `isMajiangConsoleLoggingEnabled()` 控制，Web session 可静默底层日志
- `src/majiang/application/game-session.ts`
  - Web 会话初始化时设置 `silentOutput: true`、`fatalMode: 'throw'`、`fileLoggingEnabled: false`、`consoleLoggingEnabled: false`
- `src/majiang/web/public/index.html`
  - 为 `Lobby`、`Table`、`ResultModal`、开始按钮、再来一局按钮、玩家座位、状态横幅补充稳定 `data-testid`
- `src/majiang/web/public/app.js`
  - 为人类可点击手牌与响应动作按钮补充稳定 `data-testid`
- `src/majiang/web/public/styles.css`
  - 补充按钮/手牌焦点态、触控最小高度与移动端人类手牌横向滚动
- `.gitignore`
  - 忽略 `.playwright-cli/` 浏览器验收本地产物，避免临时截图/快照误提交

### 本轮验证结果

- `npm run majiang:web-smoke`
  - 通过
  - 覆盖 `health`、`create-session`、`get-session`、`discard`、`restart`、`static-index`、`static-app-js`
  - Web smoke 输出不再夹带底层麻将流程日志
- `npm run majiang:smoke`
  - 通过
  - 覆盖 CLI 手动模式起局与 CLI 自动模式推进
  - CLI 默认仍保留原有控制台日志输出
- `npm run majiang:verify`
  - 通过
  - 顺序执行 `majiang:smoke` 与 `majiang:web-smoke`
- `npm run build`
  - 未通过
  - 原因：当前本地 `node_modules/@types` 仅包含 `node`，但 `tsconfig.json` 中声明了 `types: ["node", "mocha", "chai"]`
  - `package-lock.json` 已声明 `@types/mocha` 与 `@types/chai`，判断为本地依赖安装不完整；本轮未执行依赖安装，避免在未确认时引入网络/安装动作
- Codex 内置浏览器真实页面验收
  - 通过
  - 访问 `http://127.0.0.1:4011/majiang-web`
  - 确认 `Lobby -> Table -> 人类出牌 -> AI 推进 -> 回到人类回合`
  - 继续推进 28 步到 `ResultModal`，页面状态为 `ENDED`
  - 点击“再来一局”后回到 `PLAYING`，结果弹层关闭，人类手牌恢复 14 张可点击
  - 麻将页面控制台错误/警告为 0
- Codex 内置浏览器 `data-testid` 快速验收
  - 通过
  - `majiang-lobby`、`start-game`、`majiang-table`、`human-tile` 均可定位
  - 通过 `human-tile` 点击后可推进出牌链路
  - 麻将页面控制台错误/警告为 0
