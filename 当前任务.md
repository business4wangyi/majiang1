# 当前任务

## 任务目标

基于 [`docs/majiang/UI_TECHNICAL_DESIGN.md`](/Users/felixfan/Desktop/AIUse/majiang1-ui/docs/majiang/UI_TECHNICAL_DESIGN.md) 将麻将首版 Web UI MVP 收口到可正式发布状态。

本次发布口径采用方案 A，不使用方案 B：

- 发布对象是麻将首版 Web UI MVP
- 正式发布门禁包含全仓 `npm run build`
- Web 宿主为仓内页面，入口是 `npm run majiang:web`
- 发布页面范围只包含 `Lobby`、`Table`、`ResultModal`
- 支持 1 人 + 3 AI 的本地单机麻将流程
- 复用现有 `src/majiang/core/`、`src/majiang/strategy/agents/ai-player.ts` 与 `GameEventHandler`
- 保持 CLI 手动模式与自动模式 smoke 不回归

依据规范：

- [`AGENTS.md`](/Users/felixfan/Desktop/AIUse/majiang1-ui/AGENTS.md)
- [`docs/standards/rules/development.md`](/Users/felixfan/Desktop/AIUse/majiang1-ui/docs/standards/rules/development.md)
- [`docs/majiang/FOLDER_STRUCTURE.md`](/Users/felixfan/Desktop/AIUse/majiang1-ui/docs/majiang/FOLDER_STRUCTURE.md)

说明：

- `FOLDER_STRUCTURE.md` 仅作为目录规范目标，不视为当前代码已完全达到的事实。
- 本轮已修复阻断全仓 TypeScript 构建的历史导入/API 漂移问题，`npm run build` 已通过。
- 构建修复属于发布门禁收口，不改变麻将 Web UI/CLI 主流程语义，不重写麻将规则引擎。

## 正式发布状态

当前结论：麻将首版 Web UI MVP 正式发布门禁通过。

发布范围内已完成：

- `Lobby`
  - 支持真实模式选择
  - `manual` 可用
  - `auto-demo` 已识别但以禁用态展示为暂未开放
- `Table`
  - 展示四家布局、本人手牌、他家牌背/数量、弃牌区、明牌区、当前轮转与可行动作
  - 支持人类出牌与响应动作
  - AI 回合由 session 推进到再次需要人类输入或游戏结束
- `ResultModal`
  - 展示赢家、结算类型、最小分数摘要
  - 支持“再来一局”并回到可玩状态
- 发布门禁
  - `npm run build` 已通过
  - `npm run majiang:web-typecheck` 已通过
  - `npm run majiang:release-check` 已通过，且已顺序包含 `build`、专项 typecheck 与 smoke
- 文档入口
  - `README.md` 已说明麻将 Web UI MVP 启动方式、正式发布检查命令与 A 方案口径
  - `docs/majiang/UI_TECHNICAL_DESIGN.md` 已更新为正式发布口径

不纳入本次 MVP 发布范围：

- 非 MVP 页面或能力，例如 `RuleDrawer`、`DebugPanel`、时间线、动画、自动演示模式开放

## 正式发布验收清单

- Web 启动方式
  - 结果：通过
  - 证据：`npm run majiang:web` 启动 `src/majiang/web/server.ts`，默认访问 `http://127.0.0.1:4010/majiang-web`
- Web 主流程可用
  - 结果：通过
  - 证据：真实浏览器已验证 `Lobby -> Table -> 人类出牌 -> AI 推进 -> 回到人类回合`
- `Lobby -> Table -> ResultModal -> 再来一局` 闭环
  - 结果：通过
  - 证据：真实浏览器已推进到 `ResultModal`，点击“再来一局”后恢复 `PLAYING`
- CLI 不回归
  - 结果：通过
  - 证据：`npm run majiang:smoke` 通过，覆盖 CLI 手动模式起局与自动模式推进
- 规则引擎未重写
  - 结果：通过
  - 证据：Web 仍复用 `core/`、`strategy/agents/ai-player.ts`、`GameEventHandler`
- 静态资源主路径可访问
  - 结果：通过
  - 证据：`npm run majiang:web-smoke` 覆盖 `index.html`、`app.js`、`styles.css`、`favicon.ico`
- 页面控制台无错误
  - 结果：通过
  - 证据：Codex 内置浏览器真实页面验收确认麻将页面控制台错误/警告为 0
- 构建/验证门禁
  - 结果：通过
  - 证据：`npm run build` 通过；`npm run majiang:release-check` 通过

## 本轮执行记录

已完成：

- 将 `npm run majiang:release-check` 调整为正式发布门禁：
  - 先执行 `npm run build`
  - 再执行 `npm run majiang:web-typecheck`
  - 最后执行 `npm run majiang:verify`
- 修复全仓 `npm run build` 阻断项：
  - `src/tic-tac-toe/` 增加历史路径 re-export，恢复 `src/ai-assistant/` 旧导入兼容
  - `src/majiang/core/player.ts` 增加历史训练/分析模块需要的只读访问器
  - `src/majiang/strategy/ai-alphazero/` 增加缺失入口导出并修复网络初始化字段
  - `src/othello/` 修复测试/工具中已漂移的导入路径与 API 调用
- 更新 `docs/majiang/UI_TECHNICAL_DESIGN.md`：
  - 文档状态改为麻将首版 Web UI MVP 正式发布通过
  - 明确采用方案 A，不使用方案 B
  - 明确全仓 `npm run build` 已纳入发布门禁并通过
- 更新 `README.md`：
  - 补充麻将 Web UI MVP 正式交付入口
  - 补充正式发布检查命令
  - 删除与方案 A 不一致的阶段性发布口径

实际验证结果：

- `npm run build`
  - 结果：通过
  - 覆盖：全仓 TypeScript 编译
- `npm run majiang:release-check`
  - 结果：通过
  - 覆盖：全仓 `build`、麻将 Web UI 专项 TypeScript 检查、CLI smoke、Web smoke
  - 备注：普通沙箱执行时曾因 `dist/` 写入权限出现 `TS5033 EPERM`；提权后同一命令完整通过，判定为当前执行环境权限限制，不是 TypeScript 类型错误或发布代码缺陷。
- `npm run majiang:smoke`
  - 结果：通过
  - 覆盖：CLI 手动模式起局与 CLI 自动模式单局推进
- `npm run majiang:web-smoke`
  - 结果：通过
  - 覆盖：`health`、`manual` 建局、`auto-demo` 暂未开放、非法模式拒绝、`get-session`、`discard`、`restart`、`index.html`、`app.js`、`styles.css`、`favicon.ico`
- Python Playwright 真实页面验收
  - 结果：此前已通过
  - 覆盖：`manual` 默认选中、`auto-demo` 禁用、进入 `Table`、点击人类手牌、页面保持 `PLAYING`、控制台错误/警告为 0
